[phases.setup]
nixPkgs = ['python312', 'gcc']

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Build phase complete"']

[start]
cmd = 'gunicorn api_server:app --bind 0.0.0.0:$PORT --timeout 120 --workers 1'
